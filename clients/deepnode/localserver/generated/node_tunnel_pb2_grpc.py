# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc
import warnings

from . import node_tunnel_pb2 as node__tunnel__pb2

GRPC_GENERATED_VERSION = '1.78.0'
GRPC_VERSION = grpc.__version__
_version_not_supported = False

try:
    from grpc._utilities import first_version_is_lower
    _version_not_supported = first_version_is_lower(GRPC_VERSION, GRPC_GENERATED_VERSION)
except ImportError:
    _version_not_supported = True

if _version_not_supported:
    raise RuntimeError(
        f'The grpc package installed is at version {GRPC_VERSION},'
        + ' but the generated code in node_tunnel_pb2_grpc.py depends on'
        + f' grpcio>={GRPC_GENERATED_VERSION}.'
        + f' Please upgrade your grpc module to grpcio>={GRPC_GENERATED_VERSION}'
        + f' or downgrade your generated code using grpcio-tools<={GRPC_VERSION}.'
    )


class NodeTunnelServiceStub(object):
    """─────────────────────────────────────────────────
    NodeTunnelService — client ↔ nodemanager 控制面双向流隧道

    设计目标：
    1. 长连接：client 推理服务启动后建立并维持 TCP 级长连接
    2. 安全鉴权：连接建立时首条消息完成身份验证
    3. 心跳保活：双向定时心跳，超时自动剔除
    4. 任务通道：nodemanager 下发推理任务，client 回传结果（支持流式）
    5. 极致性能：基于 gRPC 双向流，避免频繁连接建立开销
    ─────────────────────────────────────────────────
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.Tunnel = channel.stream_stream(
                '/deeppool.nodetunnel.v1.NodeTunnelService/Tunnel',
                request_serializer=node__tunnel__pb2.ClientMessage.SerializeToString,
                response_deserializer=node__tunnel__pb2.ServerMessage.FromString,
                _registered_method=True)


class NodeTunnelServiceServicer(object):
    """─────────────────────────────────────────────────
    NodeTunnelService — client ↔ nodemanager 控制面双向流隧道

    设计目标：
    1. 长连接：client 推理服务启动后建立并维持 TCP 级长连接
    2. 安全鉴权：连接建立时首条消息完成身份验证
    3. 心跳保活：双向定时心跳，超时自动剔除
    4. 任务通道：nodemanager 下发推理任务，client 回传结果（支持流式）
    5. 极致性能：基于 gRPC 双向流，避免频繁连接建立开销
    ─────────────────────────────────────────────────
    """

    def Tunnel(self, request_iterator, context):
        """Tunnel 双向流：承载注册鉴权、心跳、任务下发与结果回传
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_NodeTunnelServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'Tunnel': grpc.stream_stream_rpc_method_handler(
                    servicer.Tunnel,
                    request_deserializer=node__tunnel__pb2.ClientMessage.FromString,
                    response_serializer=node__tunnel__pb2.ServerMessage.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'deeppool.nodetunnel.v1.NodeTunnelService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('deeppool.nodetunnel.v1.NodeTunnelService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class NodeTunnelService(object):
    """─────────────────────────────────────────────────
    NodeTunnelService — client ↔ nodemanager 控制面双向流隧道

    设计目标：
    1. 长连接：client 推理服务启动后建立并维持 TCP 级长连接
    2. 安全鉴权：连接建立时首条消息完成身份验证
    3. 心跳保活：双向定时心跳，超时自动剔除
    4. 任务通道：nodemanager 下发推理任务，client 回传结果（支持流式）
    5. 极致性能：基于 gRPC 双向流，避免频繁连接建立开销
    ─────────────────────────────────────────────────
    """

    @staticmethod
    def Tunnel(request_iterator,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.stream_stream(
            request_iterator,
            target,
            '/deeppool.nodetunnel.v1.NodeTunnelService/Tunnel',
            node__tunnel__pb2.ClientMessage.SerializeToString,
            node__tunnel__pb2.ServerMessage.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
