server:
  addr: ":8082"
  grpc_addr: ":9092"
  tls:
    enabled: false
    cert_file: ""
    key_file: ""
    skip_verify: false

log:
  level: "info"

mysql:
  host: "rm-f8z04393v8584kubu.mysql.rds.aliyuncs.com"
  port: 3306
  user: "deeppool"
  password: "AliRoot@123"
  database: "deeppool"
  max_open_conns: 20
  max_idle_conns: 10
  conn_max_lifetime_minutes: 30

# Stream-level liveness timeouts shared with the Gateway. Applied to:
#   - NodeManager DispatchStream (DeepNode chunk channel)
#   - Gateway gRPC stream to NodeManager
#   - Gateway Provider SSE
# 0 means "use code default" (10s / 20s).
stream_timeouts:
  first_token_timeout_seconds: 10
  inter_chunk_idle_timeout_seconds: 20
